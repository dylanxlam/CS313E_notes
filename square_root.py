import sys

def main():
    n = int(sys.stdin.readline9())
    lowPointer = 1
    highPointer = n
    midPointer = (lowPointer + highPointer) / 2
    tolerance = 1e - 6

while (highPointer - (mindPointer**2)) > tolerance:

